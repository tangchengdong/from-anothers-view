# LLM 配置（OpenAI 兼容接口，支持 DeepSeek/智谱/OpenAI）
# 不填则自动降级到规则模板，不影响核心功能
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# JWT 密钥（生产环境务必修改）
JWT_SECRET=dev-secret-key-change-me

# 前端地址（CORS 配置）
FRONTEND_URL=http://localhost:3000

# 后端端口
PORT=8000
